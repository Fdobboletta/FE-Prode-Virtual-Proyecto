query GetMatchesByRoomIdForPlayers($roomId: String!) {
  getMatchesByRoomIdForPlayers(roomId: $roomId) {
    id
    homeTeam
    awayTeam
    officialScore
    roomId
    startDate
    userForecast
  }
}
